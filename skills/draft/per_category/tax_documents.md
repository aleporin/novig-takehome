Point to the in-app Documents section (and the Activity/CSV export if it exists).
1099s are generated only when the user crossed the reporting threshold. Do not
invent an export feature — if unsure, say ops will resend.
